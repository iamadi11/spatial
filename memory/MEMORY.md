# Memory Index

This directory contains persistent memory files for the Spatial project. Each file encodes a standing instruction, preference, or feedback rule that applies across sessions.

| File | Type | Description |
|------|------|-------------|
| `feedback_graph_update_on_commit.md` | feedback | After every git commit, update `code-review-graph.html` with new nodes and edges |
